#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int a = 48;
int c = 97;

while (a <= 57)
{
putchar(a);
a++;
}
while (c <= 102)
{
putchar(c);
c++;
}
putchar(10);
return (0);
}
