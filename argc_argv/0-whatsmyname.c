#include <stdio.h>
/**
* main - check the code
* @argc: the len of the string
* @argv: pointer of an array
*
* Return: always 0.
*/
int main(int argc, char *argv[])
{
if (argc > 0)
printf("%s\n", argv[0]);
return (0);
}
